# sample websocket based on fastapi

## setup environment

```shell
poetry install
```

## run

```shell
poetry run uvicorn fastapi_websocket.main:api --host 0.0.0.0 --port 8000
```

access to http://localhost:8000 on your browser.
