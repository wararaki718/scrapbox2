# nextjs typescript config sample2

## build

development setting

```shell
docker-compose build
```

other environment setting

ex (production):

```shell
docker-compose -f docker-compose.yml -f docker-compose.prd.yml build
```

## start

```shell
docker-compose up
```
